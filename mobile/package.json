{
  "name": "ai-teacher-intelligence-mobile",
  "version": "6.0.0",
  "private": true,
  "description": "Android wrapper for AI Teacher Intelligence",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build:android": "npx cap copy android && npx cap sync android && npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^7.0.2"
  }
}
